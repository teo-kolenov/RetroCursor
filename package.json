{
  "name": "cursor-widget",
  "version": "1.0.0",
  "description": "Figma plugin for inserting retro cursors to a canvas",
  "main": "code.js",
  "scripts": {
    "build": "tsc",
    "test": "node --test",
    "watch": "tsc -w",
    "dev": "tsc -w"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.84.0",
    "typescript": "^5.0.0"
  }
}
